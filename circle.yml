machine:
  timezone:
    Asia/Tokyo
  golang:
    version: 1.5
  environment:
    GOPATH: $HOME/Tmtproto

dependencies:
  pre:
    - bash make.sh clean
  post:
    - go get github.com/msteinert/pam

test:
  pre:
  post:
    - bash make.sh build
