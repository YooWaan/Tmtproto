package auth

/*
import (
	"os"
	"io/ioutil"
	"testing"
	"types"
)

/*
func TestPAM(t *testing.T) {
	log := types.NewAppLogger(ioutil.Discard, os.Stdout, os.Stdout, os.Stderr)
	var cb *UserPassCallback = &UserPassCallback{log:log}
	err := cb.PamAuth("test.user", "password")
	if err != nil {
		t.Error("err = ", err)
	} else {
		t.Log("ok")
	}
}
*/



